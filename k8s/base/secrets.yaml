# Create this secret with your actual API key:
#   kubectl create secret generic agent-secrets \
#     --namespace argo \
#     --from-literal=anthropic-api-key=sk-ant-...
#
# This file is a placeholder for reference only.
apiVersion: v1
kind: Secret
metadata:
  name: agent-secrets
  namespace: argo
  labels:
    app.kubernetes.io/part-of: night-city-agents
type: Opaque
stringData:
  anthropic-api-key: "REPLACE_ME"
